# VITE_API_URL is the URL of your deployed backend on Render.
# For local development, leave this file empty or do not create an .env file,
# and it will default to http://localhost:8000.

# When deploying to Vercel, set this environment variable in the Vercel dashboard:
VITE_API_URL=https://your-render-backend-url.onrender.com
